# Full tech stack per project spec Section 8.1 (Nifty100_Project_Document_FINAL.pdf).
# This is the authoritative list — not a guess. ~20 libraries as D01 expects.

pandas>=2.0            # DataFrame ops, Excel read/write
numpy>=1.24             # Vectorised KPI computation
openpyxl>=3.1            # Read .xlsx (header=1 for core datasets)
scipy>=1.11               # Statistical tests (Sprint 3+)
scikit-learn>=1.3          # KMeans clustering, percentile ranking (Sprint 6)
matplotlib>=3.7            # Static charts for PDF reports (Sprint 5)
plotly>=5.18                 # Interactive charts for Streamlit (Sprint 4)
streamlit>=1.30                # Dashboard, 8 screens (Sprint 4)
fastapi>=0.110                   # REST API, 16 endpoints (Sprint 6)
uvicorn>=0.27                      # ASGI server for FastAPI (Sprint 6)
reportlab>=4.1                       # PDF generation — tearsheets, sector reports (Sprint 5)
nltk>=3.8                              # Tokenisation for pros/cons generation (Sprint 5)
pytest>=7.4                              # Unit tests — ETL, KPI, edge cases
pytest-html>=4.0                         # HTML test reporting for make test
jupyter>=1.0                             # Exploratory notebook (not production path)
black>=24.0                                  # Code formatting
ruff>=0.4                                      # Linting
pre-commit>=3.7                                  # Git hooks: black + ruff before commit
python-dotenv>=1.0                                 # Config via .env
requests>=2.30                                       # URL validation (DQ-13, documents.xlsx)
